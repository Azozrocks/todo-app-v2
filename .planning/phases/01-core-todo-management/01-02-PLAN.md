---
phase: 01-core-todo-management
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - bin/todo.js
  - test/list.test.js
  - test/done.test.js
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - STOR-02
  - STOR-03
user_setup: []

must_haves:
  truths:
    - "`node bin/todo.js list` shows only pending (done===false) items, numbered from 1 in array order"
    - "`node bin/todo.js list` with zero pending items prints a friendly non-empty message and exits 0"
    - "`node bin/todo.js done <n>` marks the nth pending item done and it no longer appears in list"
    - "A completed item remains in todos.json flagged done:true (retained, not deleted)"
    - "`done` with a non-numeric or out-of-range index fails with non-zero exit and a stderr message"
  artifacts:
    - "list command handler in bin/todo.js (D-02 numbered lines, D-03 empty-state message, STOR-03 pending-only)"
    - "done command handler in bin/todo.js (D-04 confirmation, STOR-02 soft-delete, CORE-03 positional index)"
    - "test/list.test.js covering pending-only + empty-state"
    - "test/done.test.js covering soft-delete + retention + invalid index"
  key_links:
    - "done <n> maps 1-based pending position -> the nth done===false element in array order (same order list prints)"
    - "list filters done===false so completed items are hidden by default (STOR-03) while still stored (STOR-02)"
---

<objective>
Complete the core add/list/done loop on top of the walking skeleton: add the `list` and `done` command handlers so users can view pending todos and mark them complete via positional index, with completed items soft-deleted (retained + flagged) rather than removed.

Purpose: Delivers the "view" and "complete" halves of the phase user story; finishes all Phase 1 success criteria.
Output: `list` + `done` handlers in bin/todo.js, test/list.test.js, test/done.test.js.
</objective>

## Phase Goal

**As a** command-line user, **I want to** add, view, and complete to-do items that persist per working directory, **so that** I can track tasks from any folder with zero setup.

(This plan delivers the `list` + `done` slices; the `add` slice + scaffold came from plan 01-01.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-todo-management/01-CONTEXT.md
@.planning/phases/01-core-todo-management/01-SKELETON.md
@.planning/phases/01-core-todo-management/01-01-SUMMARY.md
@bin/todo.js
@lib/storage.js
</context>

## Artifacts this phase produces

Consumed from plan 01-01 (do not recreate): `lib/storage.js` (`getTodosPath`, `readTodos`, `writeTodos`), `bin/todo.js` dispatch skeleton + `add`, `todos.json` shape `{ text: string, done: boolean }`.

Added by this plan (01-02):
- `bin/todo.js` `list` handler — reads todos, filters `done === false`, prints `${position}. ${text}` numbered from 1 (D-02, STOR-03); zero pending -> friendly message (D-03); exit 0.
- `bin/todo.js` `done <n>` handler — validates `n`, maps 1-based pending position to the underlying array element, sets `done = true`, writes back (retains the item — STOR-02, soft-delete), prints `Done.` (D-04); invalid/out-of-range index -> stderr + exit 1.
- `test/list.test.js`, `test/done.test.js`.

CLI commands after this plan (full Phase 1 surface): `add <text>` -> `Added.`; `list` -> numbered pending or friendly empty message; `done <n>` -> `Done.`.
Exit codes: `0` success; `1` on invalid input (empty add text, non-numeric/out-of-range `done` index) or corrupt todos.json.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the list command (view pending, numbered)</name>
  <files>bin/todo.js, test/list.test.js</files>
  <read_first>
    - bin/todo.js (existing dispatch + add handler from 01-01 — replicate its style)
    - lib/storage.js (readTodos signature and return shape)
    - .planning/phases/01-core-todo-management/01-CONTEXT.md (D-02 numbered lines `1. buy milk`; D-03 friendly empty-state message)
    - .planning/REQUIREMENTS.md (CORE-02, STOR-03)
  </read_first>
  <behavior>
    - todos.json = [{text:"buy milk",done:false},{text:"call mom",done:false}] -> `list` prints two lines "1. buy milk" then "2. call mom", exit 0.
    - todos.json with a mix, e.g. [{text:"a",done:true},{text:"b",done:false}] -> `list` prints only "1. b" (done item hidden, and numbering restarts at 1 over pending only) (STOR-03).
    - todos.json = [] OR all items done -> `list` prints a single friendly message (e.g. "No pending todos.") to stdout, exit 0 (D-03) — NOT empty output, NOT an error.
  </behavior>
  <action>
    Add a `"list"` case to the dispatch in bin/todo.js. Handler: `const todos = readTodos(); const pending = todos.filter(t => t.done === false);` If `pending.length === 0`, `console.log` a friendly all-caught-up message (implementer's exact wording per D-03; must be non-empty human text) and return/exit 0. Otherwise iterate pending with index and `console.log(\`${i + 1}. ${pending[i].text}\`)` — one line per pending item, 1-based numbering, plain text exactly matching the D-02 format `N. <text>` (no bullets, no status markers, done items excluded per STOR-03). Do not echo done items. Preserve array order (the order add appended them), because `done <n>` in Task 2 relies on the same ordering.

    Create test/list.test.js using only node built-ins (node:test, node:assert/strict, node:child_process execFileSync, node:fs, node:os, node:path). Tests: (a) seed a temp todos.json (fs.writeFileSync with two pending items), run `list`, assert stdout lines are "1. buy milk" and "2. call mom" in order; (b) seed one done + one pending, assert stdout shows only the pending item numbered "1. ..." and does not contain the done item's text; (c) seed [] (or all-done), assert stdout is the friendly non-empty message and exit code 0. Use a fresh mkdtempSync dir per test and clean up.
  </action>
  <verify>
    <automated>node --test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test` passes including the new test/list.test.js.
    - For a seeded file with 2 pending + 1 done, `node bin/todo.js list` prints exactly the 2 pending items, numbered "1." and "2.", and the done item's text is absent from stdout (STOR-03, CORE-02).
    - For an empty or all-done file, `node bin/todo.js list` prints a non-empty friendly message and exits 0 (D-03) — stdout is not blank and exit code is 0.
    - Numbering is 1-based over the pending view only (a leading done item does not consume number 1).
  </acceptance_criteria>
  <done>Users can view pending todos as a clean numbered list, with a friendly empty state and done items hidden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the done command (soft-delete by positional index)</name>
  <files>bin/todo.js, test/done.test.js</files>
  <read_first>
    - bin/todo.js (existing dispatch + add + list handlers)
    - lib/storage.js (readTodos / writeTodos)
    - .planning/phases/01-core-todo-management/01-CONTEXT.md (D-04 plain confirmation, no item text echoed; discretion: invalid index fails gracefully)
    - .planning/REQUIREMENTS.md (CORE-03, STOR-02)
  </read_first>
  <behavior>
    - Given two pending items [a, b], `done 1` -> item a gets done:true, stdout "Done.", exit 0; a remains present in todos.json (retained, STOR-02); a subsequent `list` shows only "1. b".
    - `done 2` on [a(pending), b(pending)] targets b (the 2nd pending in array order).
    - `done 0`, `done 99` (out of range), `done abc` (non-numeric), and `done` (missing arg) each -> clear stderr message + exit 1, and todos.json is left unchanged.
    - The positional index maps to the pending (done===false) view in the same order `list` prints — not the raw array index — so a leading done item does not shift the mapping.
  </behavior>
  <action>
    Add a `"done"` case to the dispatch in bin/todo.js. Handler: parse `const n = Number(rest[0]);` Validate `Number.isInteger(n) && n >= 1`; if invalid (missing, non-numeric, non-integer, or < 1), console.error a clear message (e.g. that a valid position number from `list` is required) and `process.exit(1)`. Then `const todos = readTodos(); const pending = todos.filter(t => t.done === false);` If `n > pending.length`, console.error a clear out-of-range message and `process.exit(1)` (leave the file unchanged). Otherwise take `const target = pending[n - 1];` set `target.done = true;` (mutating the same object referenced in the todos array — do NOT remove it, STOR-02 soft-delete), then `writeTodos(todos);` and `console.log('Done.');` (D-04 — plain confirmation, no item text echoed). Because `pending` holds references into `todos`, mutating `target.done` updates the array element that writeTodos persists.

    Create test/done.test.js using only node built-ins (node:test, node:assert/strict, node:child_process execFileSync, node:fs, node:os, node:path), fresh mkdtempSync dir per test. Tests: (a) seed two pending items, run `done 1`, assert stdout includes "Done." and exit 0; re-read todos.json and assert the first item now has done:true AND is still present (array length unchanged) (STOR-02); (b) after `done 1`, run `list` and assert the completed item's text no longer appears; (c) invalid indices — `done 0`, `done 99`, `done abc`, and bare `done` — each assert a non-zero exit and that todos.json is byte-for-byte unchanged from the seed.
  </action>
  <verify>
    <automated>node --test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test` passes including test/done.test.js and all prior suites (add, list) stay green.
    - After `node bin/todo.js done 1` on a two-pending seed: stdout includes "Done.", exit 0; todos.json still contains that item with done:true and total item count unchanged (STOR-02 — retained, not deleted).
    - A subsequent `node bin/todo.js list` no longer shows the completed item (CORE-03 round-trip).
    - `done 0`, `done 99`, `done abc`, and bare `done` each exit 1, print to stderr, and leave todos.json unchanged.
    - The nth-pending mapping is over the done===false view (verified by a seed with a leading done item where `done 1` targets the first pending, not the done, item).
  </acceptance_criteria>
  <done>The full add/list/done loop works: items can be completed by position, completed items are soft-deleted (retained + flagged) and disappear from the pending list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell/argv -> CLI | User-supplied `done` index enters untrusted (may be non-numeric, negative, or out of range) |
| CLI -> filesystem | Reads and rewrites todos.json in the current working directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | done handler index parsing | medium | mitigate | Validate Number.isInteger(n) && n >= 1 && n <= pending.length before any mutation; invalid -> stderr + exit 1, file left unchanged |
| T-01-06 | Information Disclosure | list handler leaking completed items | low | mitigate | list filters done === false so completed items are never printed (STOR-03) |
| T-01-07 | Tampering | done handler mutating the wrong element | medium | mitigate | Map position over the filtered pending view (references into the array), not the raw array index, so a leading done item cannot shift the target |
| T-01-08 | Denial of Service | Repeated writes / large todos.json | low | accept | Local single-user tool on a user-owned file; no untrusted/network input |
| T-01-SC | Tampering | npm/dependency supply chain | low | accept | Zero npm dependencies (Node built-ins only); no install step — nothing to audit |

No HIGH-severity threats (block-on threshold is `high`); nothing blocks execution.
</threat_model>

<verification>
- `node --test` passes with add, list, and done suites all green.
- Manual loop smoke: in a temp dir, `add "a"`, `add "b"`, `list` (shows 1. a / 2. b), `done 1`, `list` (shows 1. b), and confirm todos.json still holds `a` with done:true.
- Invalid-index smoke: `done 5` on a 2-item pending list exits 1 with stderr and does not alter todos.json.
</verification>

<success_criteria>
- `list` shows only pending items numbered from 1 (CORE-02, STOR-03, D-02); empty pending -> friendly message (D-03).
- `done <n>` marks the nth pending item done via positional index (CORE-03, D-04); completed items retained in todos.json flagged done (STOR-02).
- Invalid/out-of-range/non-numeric `done` index -> non-zero exit + stderr, file unchanged.
- All Phase 1 ROADMAP success criteria (1-5) are satisfied when combined with plan 01-01.
</success_criteria>

<output>
Create `.planning/phases/01-core-todo-management/01-02-SUMMARY.md` when done.
</output>
